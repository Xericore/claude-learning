[
  {
    "task": "Parse an AWS S3 bucket name from an S3 URI in the format 's3://bucket-name/key/path'",
    "format": "regex"
  },
  {
    "task": "Create a JSON configuration object for an AWS Lambda function that includes function name, runtime, role ARN, timeout, and memory size",
    "format": "json"
  },
  {
    "task": "Write a Python function that validates an AWS IAM role ARN format and returns True if valid, False otherwise",
    "format": "python"
  }
]