[
  {
    "task": "Write a Python function that extracts the AWS region from an S3 bucket URL in the format 's3://bucket-name.region.amazonaws.com'. The function should return just the region code (e.g., 'us-east-1')."
  },
  {
    "task": "Create a JSON object that represents an AWS IAM policy allowing read-only access to a specific S3 bucket named 'my-data-bucket'. Include the necessary Action, Resource, and Effect fields."
  },
  {
    "task": "Write a regular expression that matches valid AWS EC2 instance IDs, which follow the pattern of 'i-' followed by exactly 17 hexadecimal characters (e.g., 'i-0abcd1234efgh5678')."
  }
]